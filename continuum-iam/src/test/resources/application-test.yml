continuum:
  debug: true

 # h2 db
continuum-iam:
  jdbcUrl: jdbc:hsqldb:mem:continuum-iam;DB_CLOSE_DELAY=-1
  jdbcDriverClassName: org.hsqldb.jdbc.JDBCDriver

logging:
  level:
    com:
      kinotic: TRACE

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true