# YAML Reference http://www.yaml.org/refcard.html
continuum:
  discovery: sharedfs
  debug: true

continuum-gateway:
  disableIam: true

logging:
  level:
    org:
      springframework:
        web: DEBUG
      kinotic: TRACE
    io:
      vertx: TRACE

spring:
  devtools:
    livereload:
      port: 35730


elastic:
  hosts: 127.0.0.1:9200
  useSSL: false
  user:
  password:

server:
  port: 8088
